# Rust C64 Emulator 跨系统续接计划

创建日期：2026年08月11日

维护者：OpenAI Codex

## 续接位置

- GitHub：`JamesLinYJ/rust-c64-emulator`
- 开发分支：`agent/rust-wasm-modern-vm`
- 当前验收提交：`359df13`（真实 NTSC、音频门禁及其验收记录）。
- 远端 CI：[run 31515753152](https://github.com/JamesLinYJ/rust-c64-emulator/actions/runs/31515753152)，五个 job 全部成功。
- 旧 TypeScript 仓库仅作为 `legacy` remote，不改名、不归档。
- 暂不创建 GitHub Projects；进度继续维护在 Markdown 中。
- 当前分支是阶段性、可编译的交接点，不代表整份路线图已经实现。

### 2026年08月12日阶段增补（远端 CI 已验收）

- 候选提交 `a18e9d7` 已加入真实 MOS 6567R8 NTSC：65×263 周期域、247 行生产帧、完整状态恢复，
  并以 Rust 暴露的整数时钟/帧周期驱动 Worker 节拍和 PAL/NTSC 页面切换。
- AudioWorklet 增加有界启动预缓冲，只把开始播放后的实际断粮计为 underrun；生产遥测和浏览器
  门禁同时检查 underrun/overrun。
- 本地 `npm run check`、release `npm run verify:wasm`、完整 `npm run verify:reference` 和 Sites
  无 source-map 打包通过；Chromium 实测 PAL 50 FPS、NTSC 60 FPS，二者 0/120 超预算且音频
  underrun/overrun 均为零。
- Chrome/Edge 仍为 50 FPS；Firefox 功能通过并由 12 提升到 18 FPS，但不满足实时门槛；Fedora
  本机缺少 Playwright WebKit 系统库。提交 `359df13` 的远端
  [CI run 31515753152](https://github.com/JamesLinYJ/rust-c64-emulator/actions/runs/31515753152)
  已确认 Node 22/24、Rust/Wasm、Chromium UI 与跨浏览器五个 job 全部成功。
- 以上不改变 2026年08月11日的历史验收记录；M4 在 Firefox 实时性能解决前仍未完成。

### 2026年08月12日 Firefox 实时候选增补（远端 WebKit/CI 待验收）

- 根因是 Worker 的递归 `setTimeout` 触发浏览器嵌套定时器钳制，而非 C64 虚拟时钟错误；帧任务
  现由 `MessageChannel` 投递，正延迟只在计时器到期后投递消息，token 继续丢弃过期任务。
- Rust Strict 热路径只加入硬件状态驱动且逐周期等价的优化：CPU 被动读缓存、VIC 稳态/标准文本
  路径、CIA processor-clock/TOD 批处理与缓存状态验证、SID 静音周期批处理；没有软件、ROM、
  磁盘哈希或 benchmark 特判，`c64-core` 继续 `forbid(unsafe_code)`。
- 当前无 source-map Sites 产物包含 20 个文件和 326,815-byte Wasm。Google Chrome 与 Microsoft
  Edge 均为 50 host FPS、p95 2.60 ms、0/120 超预算；Mozilla Firefox 为 50 host FPS、
  p95 20.00 ms、9/120 超预算，三者均完成生产 module Worker/Wasm 与 BASIC PRG 路径。
- 本地 `npm run check`、`npm run verify:wasm`、全部固定 VICE/reSID/Rust/TypeScript 参考链通过；
  大 JSON trace runner 改为有界异步管道或临时文件 stdin，并有“不等待 EOF”的回归测试，避免
  Node 22 `spawnSync` 在当前沙箱中死锁或伪报 `EPERM`。
- Fedora 45 只有 ICU 78，而 Playwright WebKit 固定要求 ICU 74/libjpeg 8；本机 WebKit 未运行，
  因此此候选在 PR #6 远端 WebKit 与全 CI 成功前仍不能关闭 M4。

新系统恢复命令：

```powershell
git clone https://github.com/JamesLinYJ/rust-c64-emulator.git
cd rust-c64-emulator
git switch agent/rust-wasm-modern-vm
npm ci
rustup target add wasm32-unknown-unknown --toolchain 1.97.0
npm run check:rust
npm run typecheck
npm run lint
```

## 已完成并有验证证据的实现

### Foundation

- Cargo workspace 已包含 `c64-core` 与 `c64-wasm`。
- 固定 Rust 1.97.0、edition 2024、`wasm32-unknown-unknown`、wasm-pack 0.15.0、wasm-bindgen 0.2.127，并提交 `Cargo.lock`。
- `c64-core` 使用 `#![forbid(unsafe_code)]`；Wasm ABI 为粗粒度接口，不逐周期跨 JS/Wasm。
- 已定义 Strict/Turbo 请求、profile、PAL/NTSC 虚拟时间、统一页能力/版本框架和完整 save-state v2；v1 状态继续兼容。

### Rust Strict 主机核心

- NMOS 6510：全部 256 个合法与非法 opcode、decimal、JAM、IRQ/NMI、RDY、逐周期总线和 6510 处理器端口。
- PLA、统一 64 KiB RAM、ROM/隐藏 RAM、颜色 RAM、Cartridge 地址窗口和页 generation。
- MOS 6526 CIA：计时器、TOD、串口、端口、IRQ/NMI 管线和芯片修订差异。
- VIC-II PAL 6569R3 与 NTSC 6567R8 严格序列：bad line、BA/AEC、fetch、边框、精灵 DMA、碰撞、IRQ、light pen 和逐像素帧缓冲。
- SID：MOS6581/MOS8580 oscillator、combined waveform、noise/test/sync/ring、ADSR、两种滤波器、板级 RC、重采样与有界音频环。
- CIA2 已连接 Rust IEC 开集电极总线，并实现反相的主机输出及 PA6/PA7 输入。

### 1541 Rust 整机

- MOS 6522 VIA Rust 实现已通过 100,015 次 TypeScript 随机差分。
- Rust 与 TypeScript 均逐字节匹配 VICE revision 46176 的 40 个 PB7 页面和 24 个 timer/IFR 页面；参考数据来自真实 1541。
- IEC 总线使用固定容量、generation 校验和无热路径分配的开集电极聚合模型。
- 1541 VIA1 板级接线已实现 DATA/CLOCK 7406 反相、ATNA 门、设备地址位和 ATN→CA1 反相边沿。
- 16 MHz GCR 读写分离电路已实现 UE7/UF4 分频、磁通滤波、同步检测、弱磁通确定序列、写移位和 BYTE READY，并通过 100,008 次 TypeScript/Rust 差分。
- D64 35..42 轨解析、错误字节、写保护、GCR 编解码、标准原始磁道生成和扇区回解码已进入 Rust，并通过 Rust 单元测试。
- VIA2 板级接线、半轨磁盘机构、D64/G64 可写媒体、2 KiB RAM/ROM 地址译码、独立 6502 与整数主机/驱动器时钟已接入 C64Chipset/IEC。
- 完整 VICE revision 46176 `verify:drive` 已通过目录、LOAD、SAVE、format、IEC delay、write-protect、disk-change 与 HLS protection。

### 已完成的 Strict 外设与状态

- Datasette/TAP/Writable TAP 与 6510 motor/write/sense/read 接线已完成并通过 KERNAL LOAD、SAVE/新机 LOAD 和 VICE 波形参考。
- CRT、8K/16K/Ultimax、Ocean、Magic Desk、EasyFlash/AM29F040B 已完成；官方 EasyProg 1.6.3 与 Ocean CRT 参考通过。
- REU copy/fetch/swap/verify、autoload、IRQ、`$FF00` 延迟触发、VIC 优先级和页 generation 已完成；VICE QuickReu 1.1.1 的 8 个功能 PRG 全部通过。
- save-state v2 使用 `RC64VM02`、长度边界、CRC32 与精确解码，覆盖 CPU 微周期、全部芯片、IEC/1541、D64/G64、Tape、Cartridge/EasyFlash、REU/DMA 和确定性继续；v1 可载入，C64 firmware 不嵌入状态。

### 生产 Rust/Wasm Worker

- 生产 React 页面已切换到独立 module Worker 内的 Rust/Wasm Strict 整机；TypeScript 硬件不再是生产 fallback，只保留为差分 oracle。
- firmware 只在初始化时复制进 Wasm；后续按完整帧、PCM 批次、输入快照与 PRG 命令跨越粗粒度 ABI，不逐周期跨 JS/Wasm。
- 键盘矩阵、Shift Lock、双控制口和 RESTORE NMI 已接入 Rust `C64Chipset`；手机现有 `beforeinput` 捕获路径会发送同一输入快照。
- VIC RGBA 帧和 SID PCM 使用固定容量、可回收 Transferable 双缓冲；Worker 从 Rust 整数时钟元数据派生 PAL/NTSC 节拍，主线程只负责 Canvas、Web Audio 与 UI。
- Worker 已提供 run/pause/reset、输入、save/load state、diagnostics、Tape、Cartridge/EasyFlash、REU、1541 attach/detach 及 D64/G64 mount/eject 的版本化粗粒度请求；二进制结果使用精确 Transferable 副本，64 位诊断计数使用 high/low 字段。
- `dist/client` 是经过 Sites 打包并断言无 `.map`/`sourceMappingURL` 的生产产物；CI 将该确切产物传给 Chromium smoke，不再误测缺少 Wasm 的普通 Vite bundle。
- 同一生产产物已在实际 Google Chrome、Microsoft Edge、Playwright Firefox 和 WebKit 中完成 module Worker/Wasm、PAL 帧推进、暂停/单帧/恢复与 BASIC PRG 执行；Node ABI 由同一 `c64-core` 构建。Firefox 功能通过但远端仅 12 host FPS，不能算实时验收。

### 已通过的关键验证

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `cargo test --workspace --all-features`：99 个 Rust 单元测试及全部集成测试通过；另有 4096 opcode 单步向量测试通过。
- `npm run check:ts`：77 个测试文件、452 个测试及生产构建通过；TypeScript、ESLint 零警告。
- `npm run verify:wasm`：浏览器/Node Wasm 构建、Cartridge/EasyFlash、REU、Tape、1541、diagnostics、完整状态 ABI 与生产帧/PCM/输入/PRG 粗粒度边界验收通过。
- `npm run verify:reference`：CPU、CIA、VIA、Tape、1541、VIC、SID、Cartridge、REU 与程序聚合门禁通过。
- VICE VIC-II：raster IRQ、light pen 和 5 个逐像素参考帧通过。
- VICE SID noise/writeback：8 个固定参考用例通过。
- `npm run package:site` 与 `npm run verify:browser`：20 个生产文件、304,369-byte Wasm、零 source map；远端 PAL 50 FPS、p95 7.50 ms、120 帧零超预算，桌面和移动端输入/布局通过。
- `npm run verify:browser:compatibility`：Chrome 50 FPS、Edge 50 FPS、Firefox 12 FPS、WebKit 50 FPS 均完成同一生产 Worker/Wasm 与 PRG 路径；Firefox 实时性能仍是显式缺口。
- 提交 `c298841` 的完整 Worker 命令面与 `9d4f141` 的四浏览器门禁已由远端 [CI run 31507556499](https://github.com/JamesLinYJ/rust-c64-emulator/actions/runs/31507556499) 的 Node 22/24、Rust/Wasm、Chromium UI 与跨浏览器五个 job 全部验收；后续每完成一个阶段仍须重新运行对应聚合门禁。

## 尚未完成，禁止误报完成

1. 完整 Worker 命令面及 Chrome/Edge/Firefox/WebKit/Node 同核语义、真实 NTSC VIC/60 Hz 与 PAL/NTSC 零音频 underrun 已由远端 CI 验收；Firefox 50 FPS 已完成本地候选验收，但仍待 PR #6 远端 WebKit/全 CI，TypeScript oracle 不能提前删除。
2. Turbo 目前主要完成公开请求模型、槽位/时钟基础和内存 generation；BusBridge 精确退出、block cache、uop IR、lazy flags、fusion、deopt 与性能门槛尚未完成。
3. W65C816/SuperCPU、VM Enhanced 内存与 DMA 尚未实现。

## 下一阶段固定顺序

### 1. 补全生产 Wasm Worker

1. module Worker、生产 Strict、输入、BASIC PRG 和可回收 Transferable 帧/PCM 已完成；保持 TypeScript 硬件仅作显式 oracle。
2. Tape、Cartridge/EasyFlash、REU、drive media、save/load state 与 diagnostics 粗粒度命令已完成，不存在生产 fallback。
3. 真实 NTSC、PAL/NTSC 音频及 Edge/Firefox/WebKit 同核功能门禁已完成；Firefox 50 FPS 本地候选已通过，先完成 PR #6 远端 WebKit/全 CI。SAB/SIMD 只作 bit-identical 可选路径。

### 2. 完成 Turbo 语义与 uop 引擎

1. 先用简单指令执行器证明 Turbo 虚拟时间、BusBridge、VIC/REU/DMA 所有权和精确退出。
2. 加入预解码、页 guard、热后继、分支目标缓存、安全 fusion、lazy flags、延迟写回和精确 deopt；MMIO 永不推测。
3. 达到 BASIC ≥10×、纯 RAM ≥15×、通用 I/O ≥3× 后再标记 Turbo 稳定。

### 3. 扩展体系结构

1. 实现完整 W65C816/SuperCPU 与 24 位地址/Fast RAM。
2. 最后实现无自定义 opcode 的 VM Enhanced capability、内存池与 Enhanced DMA。

## 不可违反的约束

- 不修改、伪造或批量刷新历史日期；普通修改保留旧日期，新建/实质重构文件使用实际日期和作者。
- Rust、rustc、Clippy、TypeScript、ESLint、Wasm build 不允许任何 warning，也不使用全局 suppression 伪造零警告。
- 不针对软件名称、ROM、磁盘哈希或 benchmark 特判；优化只依据公开地址属性、事件和运行时热度。
- `c64-core` 保持 safe Rust；若 Wasm facade 最终确需 unsafe，必须最小隔离并增加边界测试。
- 固定外部参考版本与 SHA-256；不得用项目自身输出替代 VICE/真实硬件 oracle。
- 每项顺序：根因测试 → 实现 → 本地全门禁 → commit/push → 远端 CI；CI 通过后才关闭对应 issue。
- 未达到相应验收条件时，不宣称完整迁移、完整兼容或稳定。

## 给新系统 Codex 的续接提示词

```text
请继续完成 JamesLinYJ/rust-c64-emulator，使用分支 agent/rust-wasm-modern-vm。

先完整阅读 AGENTS.md、docs/IMPLEMENTATION_PLAN.md 和 docs/HANDOFF_2026-08-11.md，然后检查 git status、最近提交及远端 CI。不要创建 GitHub Projects，不要修改或伪造历史日期，不要重写已发布历史。

目标仍是：把现有 TypeScript C64 模拟器发展为 Rust/Wasm 的高性能、广泛兼容现代 C64 虚拟机。Rust 最终拥有 6510/65C816、RAM/ROM/PLA、总线、VIC-II、CIA、SID、1541/VIA/GCR/IEC、Datasette、Cartridge、REU/DMA、虚拟时钟和完整 save-state；TypeScript/React 只保留稳定 facade、Worker、Canvas/Web Audio、输入与 UI。Strict 周期精确，Turbo 使用公开统一架构规则，不允许软件或 benchmark 特判。

从“补全生产 Wasm Worker”继续：1541、Tape、Cartridge/EasyFlash、REU、全硬件 save-state、生产 Strict、module Worker、Transferable 帧/PCM、完整粗粒度命令及 Chrome/Edge/Firefox/WebKit/Node 同核语义已完成对应验收。提交 `359df13` 已由远端 CI 验收真实 NTSC VIC/60 Hz 和 PAL/NTSC 零音频 underrun；当前工作树已让 Firefox 达到 50 FPS，本机 Chrome/Edge/Firefox、完整参考链和无 source-map Sites 打包通过。先确认 PR #6 远端 WebKit/全 CI，再依计划完成 Turbo/BusBridge/uop、W65C816/SuperCPU 与 Enhanced DMA。

每个缺口都先写根因/差分测试。保持 c64-core forbid unsafe，Wasm ABI 粗粒度且内存安全。每次提交前至少运行 cargo fmt --all -- --check、cargo clippy --workspace --all-targets --all-features -- -D warnings、cargo test --workspace --all-features、npm run typecheck、npm run lint；阶段完成时运行 npm run check、npm run verify:wasm、npm run verify:reference 和相关浏览器门禁。任何 warning 或参考差异都必须修复，不能过滤、放宽或 fallback。TypeScript 硬件继续保留为明确的差分 oracle，不恢复生产 fallback。
```
